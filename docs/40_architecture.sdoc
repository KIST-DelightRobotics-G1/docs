[DOCUMENT]
TITLE: KIST DRL G1 아키텍처
UID: DOC-ARCH
VERSION: 0.1
DATE: 2026-09-05
CLASSIFICATION: Internal
OPTIONS:
  MARKUP: RST
  AUTO_LEVELS: On

[TEXT]
STATEMENT: >>>
.. warning::

   본 문서는 ``tools/gen_arch.py`` 가 ``docs/20_icd.sdoc`` 에서 **자동 생성** 한다.
   직접 편집하지 말 것. 편집해도 다음 생성 시 덮어써진다.

본 문서는 시스템의 컨테이너 구성과 컨테이너 간 데이터 흐름을 정의한다.
아래 다이어그램은 손으로 그린 것이 아니라 인터페이스 정의서(ICD)에서 생성된다.
각 ``[ICD]`` 노드의 ``송신 컨테이너`` / ``수신 컨테이너`` 가 화살표의 양 끝이고,
``통신 방식`` 이 선 모양이 된다.

.. code-block:: text

   python tools/gen_arch.py

ICD를 수정한 뒤 위 명령을 다시 실행하면 다이어그램이 갱신된다.
CI에서 ``python tools/gen_arch.py --check`` 를 돌리면
"ICD는 바뀌었는데 다이어그램은 안 바뀐" 상태를 빌드 실패로 잡을 수 있다.

현재 인터페이스 22건.
<<<

[[SECTION]]
TITLE: 읽는 법

[TEXT]
STATEMENT: >>>
다이어그램은 ICD 문서의 섹션(데이터 경로) 단위로 나누어 그린다.
전체를 한 장에 그리면 문서 폭에서 글씨를 읽을 수 없기 때문이다.

**선 모양**

- 실선 — DDS 토픽
- 점선 — DDS 외 통신 (HTTPS · gRPC · WebSocket)

**구현 상태** 는 도면에 표시하지 않는다. 각 인터페이스가 구현 파일에 연결되었는지는
**Source coverage** 화면과 각 ICD 노드의 ``File`` 관계에서 확인한다.

화살표 라벨은 ICD 번호이며, 토픽 상세는 각 그림 아래 표에 있다.
<<<

[[/SECTION]]

[[SECTION]]
TITLE: 음성 입력 경로 (STT)

[TEXT]
STATEMENT: >>>
.. raw:: html

   <div style="overflow-x:auto; max-width:100%;">
   <pre class="mermaid">
      flowchart TB
        subgraph onboard["Unitree G1 (NX) — Onboard"]
          n_ext_sensor_io["ext-sensor-io"]
          n_speaker_node["speaker_node"]
        end
        subgraph workstation["PC (RTX 4090) — Workstation"]
          n_stt_node["stt_node"]
          n_orchestrator_node["orchestrator_node"]
        end
        subgraph external["External"]
          n_Google_Cloud_STT["Google Cloud STT"]
        end

        n_ext_sensor_io -->|"ICD-3"| n_stt_node
        n_stt_node -.->|"ICD-33"| n_Google_Cloud_STT
        n_stt_node -->|"ICD-64"| n_orchestrator_node
        n_speaker_node -->|"ICD-42"| n_stt_node
   </pre>
   </div>

.. list-table::
   :widths: 10 22 22 46
   :header-rows: 1

   * - ID
     - 송신
     - 수신
     - 토픽 / 엔드포인트
   * - ICD-3
     - ext-sensor-io
     - stt_node
     - ``rt/kist/mic/audio``
   * - ICD-33
     - stt_node
     - Google Cloud STT
     - ``google.cloud.speech.v1``
   * - ICD-64
     - stt_node
     - orchestrator_node
     - ``/cortex/stt/transcript``
   * - ICD-42
     - speaker_node
     - stt_node
     - ``/bridge/audio/speaker_state``

<<<

[[/SECTION]]

[[SECTION]]
TITLE: 음성 출력 경로 (TTS)

[TEXT]
STATEMENT: >>>
.. raw:: html

   <div style="overflow-x:auto; max-width:100%;">
   <pre class="mermaid">
      flowchart TB
        subgraph onboard["Unitree G1 (NX) — Onboard"]
          n_speaker_node["speaker_node"]
        end
        subgraph workstation["PC (RTX 4090) — Workstation"]
          n_orchestrator_node["orchestrator_node"]
          n_tts_node["tts_node"]
        end
        subgraph external["External"]
          n_Naver_CLOVA_TTS["Naver CLOVA TTS"]
        end

        n_orchestrator_node -->|"ICD-68"| n_tts_node
        n_tts_node -.->|"ICD-34"| n_Naver_CLOVA_TTS
        n_tts_node -->|"ICD-8"| n_speaker_node
        n_orchestrator_node -->|"ICD-69"| n_tts_node
   </pre>
   </div>

.. list-table::
   :widths: 10 22 22 46
   :header-rows: 1

   * - ID
     - 송신
     - 수신
     - 토픽 / 엔드포인트
   * - ICD-68
     - orchestrator_node
     - tts_node
     - ``/cortex/tts/say``
   * - ICD-34
     - tts_node
     - Naver CLOVA TTS
     - ``REST API``
   * - ICD-8
     - tts_node
     - speaker_node
     - ``/bridge/cmd/audio_out``
   * - ICD-69
     - orchestrator_node
     - tts_node
     - ``/cortex/tts/stop``

<<<

[[/SECTION]]

[[SECTION]]
TITLE: 시나리오 실행 및 판정

[TEXT]
STATEMENT: >>>
.. raw:: html

   <div style="overflow-x:auto; max-width:100%;">
   <pre class="mermaid">
      flowchart TB
        subgraph workstation["PC (RTX 4090) — Workstation"]
          n_vlm_node["vlm_node"]
          n_orchestrator_node["orchestrator_node"]
          n_gearsonic["gearsonic"]
        end
        subgraph external["External"]
          n_VLM_API["VLM API"]
        end

        n_orchestrator_node -->|"ICD-65"| n_vlm_node
        n_vlm_node -->|"ICD-66"| n_orchestrator_node
        n_vlm_node -.->|"ICD-71"| n_VLM_API
        n_orchestrator_node -->|"ICD-70"| n_gearsonic
   </pre>
   </div>

.. list-table::
   :widths: 10 22 22 46
   :header-rows: 1

   * - ID
     - 송신
     - 수신
     - 토픽 / 엔드포인트
   * - ICD-65
     - orchestrator_node
     - vlm_node
     - ``/cortex/active_subtask``
   * - ICD-66
     - vlm_node
     - orchestrator_node
     - ``/cortex/critic/verdict``
   * - ICD-71
     - vlm_node
     - VLM API
     - ``REST API (TBD)``
   * - ICD-70
     - orchestrator_node
     - gearsonic
     - ``(미정) ActionCmd``

<<<

[[/SECTION]]

[[SECTION]]
TITLE: 모니터링 및 디스플레이

[TEXT]
STATEMENT: >>>
.. raw:: html

   <div style="overflow-x:auto; max-width:100%;">
   <pre class="mermaid">
      flowchart TB
        subgraph onboard["Unitree G1 (NX) — Onboard"]
          n_ext_sensor_io["ext-sensor-io"]
        end
        subgraph workstation["PC (RTX 4090) — Workstation"]
          n_vlm_node["vlm_node"]
          n_orchestrator_node["orchestrator_node"]
          n_gui_bridge_node["gui_bridge_node"]
        end
        subgraph external["External"]
          n_Display_Renderer["Display Renderer"]
        end

        n_ext_sensor_io -->|"ICD-62"| n_vlm_node
        n_orchestrator_node -->|"ICD-67"| n_gui_bridge_node
        n_gui_bridge_node -.->|"ICD-55"| n_Display_Renderer
   </pre>
   </div>

.. list-table::
   :widths: 10 22 22 46
   :header-rows: 1

   * - ID
     - 송신
     - 수신
     - 토픽 / 엔드포인트
   * - ICD-62
     - ext-sensor-io
     - vlm_node
     - ``rt/kist/camera/<name>/color/h264``
   * - ICD-67
     - orchestrator_node
     - gui_bridge_node
     - ``/cortex/task_status``
   * - ICD-55
     - gui_bridge_node
     - Display Renderer
     - ``ws://<cortex-host>:8081``

<<<

[[/SECTION]]

[[SECTION]]
TITLE: 센서 입력 (측위 · 인지)

[TEXT]
STATEMENT: >>>
.. raw:: html

   <div style="overflow-x:auto; max-width:100%;">
   <pre class="mermaid">
      flowchart TB
        subgraph onboard["Unitree G1 (NX) — Onboard"]
          n_G1["G1"]
          n_ext_sensor_io["ext-sensor-io"]
        end
        subgraph workstation["PC (RTX 4090) — Workstation"]
          n_nav_planner["nav-planner"]
        end

        n_ext_sensor_io -->|"ICD-36"| n_nav_planner
        n_G1 -->|"ICD-58"| n_nav_planner
        n_ext_sensor_io -->|"ICD-63"| n_nav_planner
   </pre>
   </div>

.. list-table::
   :widths: 10 22 22 46
   :header-rows: 1

   * - ID
     - 송신
     - 수신
     - 토픽 / 엔드포인트
   * - ICD-36
     - ext-sensor-io
     - nav-planner
     - ``rt/kist/uwb/pose``
   * - ICD-58
     - G1
     - nav-planner
     - ``rt/utlidar/cloud_livox_mid360``
   * - ICD-63
     - ext-sensor-io
     - nav-planner
     - ``rt/kist/camera/<name>/depth/rvl``

<<<

[[/SECTION]]

[[SECTION]]
TITLE: 저수준 제어 및 안전

[TEXT]
STATEMENT: >>>
.. raw:: html

   <div style="overflow-x:auto; max-width:100%;">
   <pre class="mermaid">
      flowchart TB
        subgraph onboard["Unitree G1 (NX) — Onboard"]
          n_G1["G1"]
        end
        subgraph workstation["PC (RTX 4090) — Workstation"]
          n_gearsonic["gearsonic"]
        end

        n_G1 -->|"ICD-4"| n_gearsonic
        n_G1 -->|"ICD-41"| n_gearsonic
        n_gearsonic -->|"ICD-50"| n_G1
        n_gearsonic -->|"ICD-6"| n_G1
   </pre>
   </div>

.. list-table::
   :widths: 10 22 22 46
   :header-rows: 1

   * - ID
     - 송신
     - 수신
     - 토픽 / 엔드포인트
   * - ICD-4
     - G1
     - gearsonic
     - ``rt/lowstate``
   * - ICD-41
     - G1
     - gearsonic
     - ``rt/secondary_imu``
   * - ICD-50
     - gearsonic
     - G1
     - ``rt/lowcmd``
   * - ICD-6
     - gearsonic
     - G1
     - ``rt/dex3/{left,right}/cmd``

<<<

[[/SECTION]]
